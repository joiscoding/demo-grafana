import { ArgTypes } from '@storybook/blocks';
import { FileDropzone } from './FileDropzone';

import { createStructuredLogger } from '@grafana/data';
const structuredLogger = createStructuredLogger('packages/grafana-ui/src/components/FileDropzone/FileDropzone');

# FileDropzone

A dropzone component to use for file uploads.

### Usage

```jsx
import { FileDropzone } from '@grafana/ui';

<FileDropzone onLoad={(result) => structuredLogger.log(result)} />;
```

### Props

<ArgTypes of={FileDropzone} />
