import { ArgTypes } from '@storybook/blocks';
import { FilterPill } from './FilterPill';

import { createStructuredLogger } from '@grafana/data';
const structuredLogger = createStructuredLogger('packages/grafana-ui/src/components/FilterPill/FilterPill');

# FilterPill

A component used for quick toggling on/off filters. Mostly used in inline form components and transformation/query editors.

### Usage

```jsx
import { FilterPill } from '@grafana/ui';

<FilterPill label={'Test'} onClick={() => structuredLogger.log('toggle')} />;
```

### Props

<ArgTypes of={FilterPill} />
